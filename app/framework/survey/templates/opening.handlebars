<div>
    <div>
        <center><img src="{{ headerImg }}" class="odk-formlogo" /></center>
        <hr class="logoDivider">
    </div>

    <table class="instanceForm">
        <tr>
            <td class="instanceFormHeading">
                <p>ODK Survey</p>
                <p>Form</p>
            </td>
            <td class="instanceFormDivider"><BR></td>
            <td class="instanceFormData">
                <p>{{localize form_title}}</p>
                <p>{{#if form_version}}{{form_version}}{{/if}}</p>
            </td>
        </tr>
    </table>

    <div class="instanceClearDiv"></div>

    <div>
        <hr>
        <p>You are at the start of Instance: </p>
        <p>"{{display_field}}"</p> 
        <hr>
        <!--
        <p>Swipe the screen as shown below to begin.</p>
        <div>
            <span style="width:0;float:left;">
                <img src="{{ backupImg }}"/>
                <p style="white-space: nowrap;">Previous prompt</p>
            </span>
            <span style="float:right;">
                <img src="{{ advanceImg }}"/>
                <p>Next prompt</p>
            </span>
        </div>
        -->
        {{#if last_save_date}}
            <p>Last saved:</p> 
            <p>{{last_save_date}}</p>
        {{/if}}
        <hr>
        <fieldset class="openingNew ui-grid-a">
            <div class="ui-block-a">
                <p>Go to next prompt</p>
            </div>
            <div class="openingNew ui-block-b">
                <button class="odk-next-btn" data-role="button" data-icon="carat-r" data-iconpos="top"></a>               
            </div>
        </fieldset>
    </div>
</div>