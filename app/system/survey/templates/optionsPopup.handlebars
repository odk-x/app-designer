<div class="modal" id="optionsPopup" tabindex="-1" aria-labelledby="optionsLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body">
                <div class="btn-group-vertical">
                    <a href="#" class="languageMenu btn btn-default"
                                data-toggle="modal" data-target="languagePopup"
                                {{#unless hasTranslations}}disabled="disabled"{{/unless}}">
                        Language
                    </a>
                    <a href="#" class="show-contents btn btn-default"
                                data-toggle="modal" data-target="languagePopup"
                                {{#unless showContents}}disabled="disabled"{{/unless}}">
                        Contents
                    </a>
                    <a href="#" class="ignore-changes-and-exit btn btn-default">
                        Ignore Changes + Exit
                    </a>
                    <a href="#" class="save-incomplete-and-exit btn btn-default">
                        Save Changes + Exit
                    </a>
                    <a href="#" class="finalize-and-exit btn btn-default">
                        Finalize Changes + Exit
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

