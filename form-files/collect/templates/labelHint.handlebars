{{#if label}}
    <div>
        {{#substitute}}{{localize label}}{{/substitute}}
    </div>
{{/if}}
{{#if image}}
    <div>
        <img width="100%" src="{{localize image}}"></img>
    </div>
{{/if}}
{{#if audio}}
    <div>
        <audio controls="controls">
            <source src="{{localize audio}}" type="audio/ogg" />
            Your browser does not support the audio element.
        </audio>
    </div>
{{/if}}
{{#if video}}
    <div>
        <video controls="controls">
            <source src="{{localize video}}" type="video/ogg" />
            Your browser does not support the video element.
        </video>
    </div>
{{/if}}
{{#if hint}}
    <p class="odk-hint">{{#substitute}}{{localize hint}}{{/substitute}}</p>
{{/if}}