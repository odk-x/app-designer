<div>
	<center>
        <img src="{{ headerImg }}" class="odk-formlogo" />
		<p>You are at the end of:</p>
        <p>"{{localize form_title}}"</p>
        <div>
            <label for="instance_name">Name this instance:</label>
            <input class="odk" name="instance_name" value="{{instance_name}}"></input>
        </div>
		<button class="incomplete">Save as Incomplete</button>
		<button class="finalize">Finalize</button>
	</center>
</div>
