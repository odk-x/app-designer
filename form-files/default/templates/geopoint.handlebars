<div class="{{#if hide}}odk-hidden{{/if}}">
    {{> labelHint}}
    <button class="captureAction">Capture Geopoint</button>
    <p class="status"></p>
    <p>Latitude: {{value.latitude}}</p>
    <p>Longitude: {{value.longitude}}</p>
</div>