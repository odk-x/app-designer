#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-open/bin/grunt-open" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-open/bin/grunt-open" "$@"
  ret=$?
fi
exit $ret
